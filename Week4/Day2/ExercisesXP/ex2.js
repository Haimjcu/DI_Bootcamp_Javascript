
function gramsd(kg) {
    return kg*1000;
}
gramsd(1.125);

let gramse = function (kg) {
    return kg*1000;
}
gramse(1.125);
// expression is a variable that is stored in memory

const grams = kg => (kg*1000);
grams(1.125)

